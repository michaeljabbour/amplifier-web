# Amplifier Web Frontend - Environment Configuration
# Copy this file to .env.local and customize for your environment
# .env.local is gitignored and won't be committed

# =============================================================================
# Server Binding
# =============================================================================

# Host to bind to (default: localhost)
# Set to 0.0.0.0 to allow external access from other machines
VITE_HOST=localhost

# Note: You can also use the CLI flag instead of this env var:
#   npm run dev -- --host 0.0.0.0
