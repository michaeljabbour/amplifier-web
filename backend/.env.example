# Amplifier Web Backend Configuration
# Copy this file to .env and fill in your values

# Server binding
HOST=0.0.0.0
PORT=4000

# CORS - comma-separated list of allowed origins
# For local development:
AMPLIFIER_WEB_ALLOWED_ORIGINS=http://localhost:4100,http://localhost:4000

# For remote access (replace with your hostname):
# AMPLIFIER_WEB_ALLOWED_ORIGINS=http://your-host:4100,http://your-host:4000

# Auth token (auto-generated on first run if not set)
# AMPLIFIER_WEB_AUTH_TOKEN=your-secure-token-here

# LLM Provider API Keys (at least one required)
ANTHROPIC_API_KEY=your-anthropic-api-key
# OPENAI_API_KEY=your-openai-api-key

# Session storage location (default: ~/.amplifier/web-sessions)
# AMPLIFIER_WEB_SESSIONS_DIR=/custom/path/to/sessions
