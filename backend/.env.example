# Amplifier Web Backend - Environment Configuration
# Copy this file to .env.local and customize for your environment
# .env.local is gitignored and won't be committed

# =============================================================================
# Server Binding
# =============================================================================

# Host to bind to (default: 127.0.0.1)
# Set to 0.0.0.0 to allow external access from other machines
HOST=127.0.0.1

# Port to listen on (default: 8000)
PORT=8000

# =============================================================================
# CORS Configuration
# =============================================================================

# Additional CORS origins to allow (comma-separated)
# Required when accessing from external hostnames
# Example: http://my-server:5173,http://my-server:8000
AMPLIFIER_WEB_ALLOWED_ORIGINS=

# =============================================================================
# Authentication
# =============================================================================

# Override the auto-generated auth token (optional)
# If not set, a token is generated and saved to ~/.amplifier/web-auth.json
# AMPLIFIER_WEB_TOKEN=your-custom-token

# =============================================================================
# LLM Providers (at least one required)
# =============================================================================

# Anthropic (recommended)
# ANTHROPIC_API_KEY=sk-ant-...

# OpenAI (alternative)
# OPENAI_API_KEY=sk-...
